# Example .env file for Survey Application Backend
# Copy this file to `.env` and fill in your actual values

# The port your server will run on
PORT=5000

# JWT secret key for signing tokens (change to a strong, random value)
JWT_KEY="your_jwt_secret_key"

# PostgreSQL database connection string (update with your credentials)
DATABASE_URL=postgresql://username:password@localhost:5432/your-database-name
